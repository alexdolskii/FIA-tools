name: Lint and Format

on:
  push:
    branches: [main, tech_dev]
  pull_request:
    branches: [main, tech_dev]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run Ruff
      uses: astral-sh/ruff-action@v3
      